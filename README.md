# Splash EAST size = Home

Home logo: `text-5xl`
Splash was: `clamp(3rem, 14vw, 4.5rem)` — too large on phone width.

Change: SplashScreen h1 → `text-5xl` (same as Home).

Overwrite: `src/components/SplashScreen.tsx`
