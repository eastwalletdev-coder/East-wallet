# Home layout: compact deposit rack + full node beside mining balance

- **To chain** — small button next to mining balance; rack **closed by default**
- **Full node** — small toggle beside it (requires Light Node connected)
- Large deposit form and fullnode block button removed from the main stack

Overwrite `src/app/page.tsx` (and FullNodeConsentDialog if missing).
